defmodule OfferService do
  @moduledoc """
  Top-level module for the Jeeb offer/auction service.

  The public surface lives under `OfferService.Auction` (domain) and
  `OfferServiceWeb` (HTTP transport). Everything else is internal.
  """
end
